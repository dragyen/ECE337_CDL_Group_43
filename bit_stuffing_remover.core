CAPI=2:
name: "u337mg158:ece337:bit_stuffing_remover:1.0.0"
description: ""

filesets:
    rtl:
        files:
            - "source/bit_stuffing_remover.sv"
        file_type: systemVerilogSource

    synth:
        depend:
            - "ece337:tech:AMI_05_LIB"
            - "u337mg158:ece337:bit_stuffing_remover_syn"

    tb:
        files:
            - "testbench/tb_bit_stuffing_remover.sv"
            - "waves/bit_stuffing_remover.do": { file_type: user }
            - "waves/bit_stuffing_remover.gtkw": { file_type: user }
        file_type: systemVerilogSource
        depend:
            - "u337mg158:ece337:questafiles"

    synfiles:
        files:
            - "scripts/syn_bit_stuffing_remover.tcl"
        file_type: tclSource
        depend:
            - "u337mg158:ece337:synfiles"

targets:
    default: &default
        filesets:
            - rtl
        toplevel: bit_stuffing_remover

    sim: &sim
        <<: *default
        default_tool: verilator
        filesets_append:
            - tb
        toplevel: tb_bit_stuffing_remover
        tools:
            modelsim:
                vsim_options:
                    - -vopt
                    - -voptargs='+acc'
                    - -t ps
                    - -do "source waves.tcl ; load_wave waves/bit_stuffing_remover.do"
                    - -onfinish stop
                    - -do "set PrefSource(OpenOnFinish) 0 ; set PrefMain(LinePrefix) \"\" ; set PrefMain(colorizeTranscript) 1"
                    - -coverage
                vlog_options:
                    - +cover
            verilator:
                verilator_options:
                    - --cc
                    - --trace
                    - --main
                    - --timing
                    - --coverage
                make_options:
                    - -j

    lint:
        <<: *default
        default_tool: verilator
        filesets_append:
            - tb
        tools:
            verilator:
                mode: lint-only
                verilator_options:
                    - --timing
                    - -Wall

    syn:
        <<: *default
        filesets_append:
            - synfiles
        default_tool: design_compiler
        toplevel: bit_stuffing_remover
        tools:
            design_compiler:
                script_dir: "src/u337mg158_ece337_synfiles_0"
                dc_script: "synth.tcl"
                report_dir: "reports"
                target_library: "/home/ecegrid/a/ece337/summer24-refactor/tech/ami05/osu05_stdcells.db"
                libs: "/home/ecegrid/a/ece337/summer24-refactor/tech/ami05/osu05_stdcells.db dw_foundation.sldb"

    syn_sim:
        <<: *sim
        filesets:
            - synth
            - tb